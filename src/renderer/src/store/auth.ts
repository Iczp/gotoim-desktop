import { defineStore } from 'pinia'

export const useAuthStore = defineStore(
  'auth',
  () => ({
    state: () => {
      return {}
    },
    getters: {},
    actions: {},
  }),
  {},
)

export default useAuthStore
